# User：项目入口与中断

这里保留每个工程都必须有、但不属于具体业务模块的项目级入口：

- `main.c/.h`：系统入口与统一错误处理。
- `stm32f10x_it.c/.h`：项目中断入口。
- `stm32f10x_conf.h`：标准外设库头文件选择。

边界：不要把 ADC、TIM、量程、电机等具体实现重新堆进 `User`；它们应进入 `Driver`、`Bsp` 或 `App`。
